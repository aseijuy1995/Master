<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nom.cp101.master.master">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Master.Master">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Message.MessageChatroom"
            android:theme="@style/MessageTheme" />
        <activity
            android:name=".CourseArticleActivity.CourseArticleActivity"
            android:theme="@style/CourseArticleCategoryTheme" />
        <activity
            android:name=".ExperienceArticleActivity.ExperienceArticleActivity"
            android:label="@string/title_activity_experience_article"

            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>